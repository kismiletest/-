local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local localPlayer = Players.LocalPlayer

-- 【核心：强力清理旧版本 UI】
-- 这段代码会寻找并删除所有之前运行过的 1.en Hub 界面，防止 V11, V12 残留
for _, oldUI in pairs(localPlayer:WaitForChild("PlayerGui"):GetChildren()) do
    if oldUI.Name:find("1enHub") then
        oldUI:Destroy()
    end
end

-- 状态控制
local ESP_ENABLED = false
local TOGGLE_KEY = Enum.KeyCode.RightControl
local _espStorage = {}

------------------------------------------------
-- 逻辑：深度玩家验证 (只看真人，不看NPC)
------------------------------------------------
local function isRealPlayer(player)
    -- 只有 UserId 大于 0 的才是真人玩家，NPC 通常为 0 或负数
    return player:IsA("Player") and player.UserId > 0
end

local function clearESP()
    for _, item in pairs(_espStorage) do
        if item and item.Parent then item:Destroy() end
    end
    _espStorage = {}
end

local function applyESP(player)
    if not isRealPlayer(player) or player == localPlayer then return end

    local function createUI(character)
        if not character then return end
        local root = character:WaitForChild("HumanoidRootPart", 10)
        if not root then return end

        -- 清理该玩家身上可能残留的旧标记
        for _, old in pairs(root:GetChildren()) do
            if old.Name == "PlayerESP_UI" then old:Destroy() end
        end

        local gui = Instance.new("BillboardGui", root)
        gui.Name = "PlayerESP_UI"
        gui.Size = UDim2.new(0, 100, 0, 40)
        gui.AlwaysOnTop = true
        gui.MaxDistance = 999999 -- 无限距离
        gui.ExtentsOffset = Vector3.new(0, 3, 0)
        table.insert(_espStorage, gui)

        local label = Instance.new("TextLabel", gui)
        label.Size = UDim2.new(1, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.TextColor3 = Color3.fromRGB(0, 255, 120)
        label.TextStrokeTransparency = 0
        label.TextScaled = true
        label.Font = Enum.Font.SourceSansBold

        task.spawn(function()
            while ESP_ENABLED and player.Parent and character.Parent and root.Parent do
                local myChar = localPlayer.Character
                if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                    local dist = (root.Position - myChar.HumanoidRootPart.Position).Magnitude
                    label.Text = string.format("%s\n%dm", player.DisplayName or player.Name, math.floor(dist))
                end
                task.wait(0.5)
            end
            if gui then gui:Destroy() end
        end)
    end

    player.CharacterAdded:Connect(createUI)
    if player.Character then createUI(player.Character) end
end

------------------------------------------------
-- UI 面板 (V13.1 强制更新)
------------------------------------------------
local ScreenGui = Instance.new("ScreenGui", localPlayer.PlayerGui)
ScreenGui.Name = "1enHub_V13_Fixed" -- 唯一标识符
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 200, 0, 140)
MainFrame.Position = UDim2.new(0.5, -100, 0.2, 0) -- 位置稍作调整，方便你区分新旧
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "  1.en Hub V13.1 (Latest)"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Title.Font = Enum.Font.SourceSansBold
Instance.new("UICorner", Title)

local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -32, 0, 2)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "✖"
CloseBtn.TextColor3 = Color3.new(1, 0.4, 0.4)
CloseBtn.TextSize = 18
CloseBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false end)

local EspBtn = Instance.new("TextButton", MainFrame)
EspBtn.Size = UDim2.new(0.9, 0, 0, 45)
EspBtn.Position = UDim2.new(0.05, 0, 0.4, 0)
EspBtn.BackgroundColor3 = Color3.fromRGB(160, 40, 40)
EspBtn.Text = "真玩家透视: 关"
EspBtn.TextColor3 = Color3.new(1, 1, 1)
EspBtn.Font = Enum.Font.SourceSansBold
Instance.new("UICorner", EspBtn)

local Hint = Instance.new("TextLabel", MainFrame)
Hint.Text = "旧版已自动清理 | 右Ctrl"
Hint.Size = UDim2.new(1, 0, 0, 20)
Hint.Position = UDim2.new(0, 0, 0.8, 0)
Hint.TextColor3 = Color3.new(0.6, 0.6, 0.6)
Hint.BackgroundTransparency = 1
Hint.TextSize = 10

------------------------------------------------
-- 绑定
------------------------------------------------
EspBtn.MouseButton1Click:Connect(function()
    ESP_ENABLED = not ESP_ENABLED
    EspBtn.Text = ESP_ENABLED and "真玩家透视: 开" or "真玩家透视: 关"
    EspBtn.BackgroundColor3 = ESP_ENABLED and Color3.fromRGB(40, 160, 40) or Color3.fromRGB(160, 40, 40)
    
    if ESP_ENABLED then
        for _, p in pairs(Players:GetPlayers()) do applyESP(p) end
    else
        clearESP()
    end
end)

UserInputService.InputBegan:Connect(function(i, p)
    if not p and i.KeyCode == TOGGLE_KEY then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

Players.PlayerAdded:Connect(applyESP)

print("1.en Hub V13.1 加载成功！旧版本 UI 已被强制销毁。")
