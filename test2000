local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "培养花园2 复制种子",
    Icon = "sprout",  -- 或其他 icon
    Theme = "Dark",   -- Dark / Light / Violet 等
})

local Tab = Window:Tab({ Title = "复制种子", Icon = "copy" })

-- 示例：复制按钮 + 参数
Tab:Button({
    Title = "开始复制当前选中种子",
    Callback = function()
        -- 这里放你的复制逻辑（firetouch 等或 Remote 调用）
        print("开始复制种子...")
        -- 示例：重复 dupe
        for i = 1, 10 do
            -- 你的 dupe 代码...
            task.wait(0.5)  -- 防检测间隔，建议 3-5 秒根据游戏调整
        end
    end
})

Tab:Slider({
    Title = "复制数量",
    Min = 1,
    Max = 100,
    Default = 10,
    Callback = function(value)
        print("设置复制数量:", value)
    end
})

Tab:Toggle({
    Title = "自动循环复制",
    Callback = function(state)
        -- 循环逻辑
    end
})
