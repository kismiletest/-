loadstring([[

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Nexxuzz Hub Pub",
    LoadingTitle = "Cagada",
    Icon = "gem",
    LoadingSubtitle = "skidded code 67",
    ConfigurationSaving = {
        Enabled = false,
        FolderName = Nexxuzz,
        FileName = "Nexxuzzzzzzzzz"
    },
    Discord = {Enabled = false},
    KeySystem = false
})

--  Variables globales 
local running               = false
local TeleportEnabled       = false
local SelectedPlayer        = nil
local TeleportConnection    = nil
local TweenSpeed            = {X = 350, Y = 350}
local SafeZoneActive        = false
local SafeZoneConnection    = nil
local OriginalPosition      = nil
local FastAttackEnabled     = false
local FastAttackConnection  = nil
local FastAttackRange       = 5000
local ESPEnabled            = false
local ESPNames              = false
local ESPDistance           = false
local ESPMaxDistance        = 99999999
local OriginalCamera        = nil
local SpectateConnection    = nil
local ActiveTween           = nil
local AutoFarmPositions     = false
local AutoFarmConnection    = nil
local AutoStunDetection     = false
local StunDetectionConnection = nil
local AutoKillEnabled       = false
local AutoKillConnection    = nil
local PREDICTION_SAMPLES    = 3
local PREDICTION_TIME       = 0.3
local PlayerPositionHistory = {}
local InstaTeleportEnabled  = false
local InstaTpConnection     = nil

--  Servicios 
local Players         = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UIS             = game:GetService("UserInputService")
local RunService      = game:GetService("RunService")
local TweenService    = game:GetService("TweenService")
local lp             = Players.LocalPlayer
local root           = lp.Character.HumanoidRootPart
local Net            = ReplicatedStorage.Modules.Net
local RegisterHit    = Net["RE/RegisterHit"]
local RegisterAttack = Net["RE/RegisterAttack"]

--  Funcin: quitar/poner colisin 
local function SetCollide()
    local character = Players.LocalPlayer.Character
    if not character then return end
    for _, v in ipairs(character:GetChildren()) do
        if v:IsA("BasePart") then
            v.CanCollide = false
            v.CanTouch   = true
            v.CanQuery   = true
        end
    end
end

local function SetNoCollide()
    local character = Players.LocalPlayer.Character
    if not character then return end
    for _, v in ipairs(character:GetChildren()) do
        if v:IsA("BasePart") then
            v.CanCollide = false
            v.CanTouch   = true
            v.CanQuery   = true
        end
    end
end

--  Funcin: Tween al HRP de un objetivo 
local function TP(targetHRP)
    local player = Players.LocalPlayer
    if not player.Character then return end

    local HRP      = player.Character:FindFirstChild("HumanoidRootPart")
    local Humanoid = player.Character:FindFirstChild("Humanoid")
    if not HRP or not Humanoid then return end

    HRP.AssemblyLinearVelocity  = Vector3.zero
    HRP.AssemblyAngularVelocity = Vector3.zero
    Humanoid:ChangeState(Enum.HumanoidStateType.Physics)

    local targetCFrame =
        CFrame.new(targetHRP.Position)
        * CFrame.Angles(0, math.rad(targetHRP.Orientation.Y), 0)
        * CFrame.new(XOffset, YOffset, ZOffset)

    local Distance       = (targetCFrame.Position - HRP.Position).Magnitude
    local horizontalDist = math.sqrt(
        (targetCFrame.Position.X - HRP.Position.X)^2 +
        (targetCFrame.Position.Z - HRP.Position.Z)^2
    )
    local verticalDist   = math.abs(targetCFrame.Position.Y - HRP.Position.Y)

    local speed = TweenSpeed.X
    if verticalDist > horizontalDist then speed = TweenSpeed.Y end

    if ActiveTween then ActiveTween:Cancel() end

    ActiveTween = TweenService:Create(
        HRP,
        TweenInfo.new(Distance / speed, Enum.EasingStyle.Linear),
        {CFrame = targetCFrame}
    )

    local isActive = true
    task.spawn(function()
        while isActive do
            SetNoCollide()
            task.wait(0.001)
        end
    end)

    ActiveTween:Play()

    ActiveTween.Completed:Connect(function()
        isActive = false
        if Humanoid then
            SetCollide()
            Humanoid:ChangeState(Enum.HumanoidStateType.Running)
        end
    end)

    task.delay(0.05, function()
        if Humanoid then
            Humanoid:ChangeState(Enum.HumanoidStateType.Running)
        end
    end)
end

--  Lista de jugadores 
local function GetPlayerList()
    local list = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Players.LocalPlayer then table.insert(list, p.Name) end
    end
    if #list == 0 then table.insert(list, "None") end
    return list
end

--  Teleport normal 
local function StartTeleporting()
    if TeleportConnection then TeleportConnection:Disconnect() end
    TeleportConnection = RunService.Heartbeat:Connect(function()
        if TeleportEnabled and SelectedPlayer and not SafeZoneActive then
            local target = Players:FindFirstChild(SelectedPlayer)
            if target and target.Character then
                local hrp = target.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    TP(hrp)
                    SetNoCollide()
                end
            end
        end
    end)
end

--  Insta-teleport (usa RunService.Stepped) 
local function StartInstaTeleporting()
    if InstaTpConnection then InstaTpConnection:Disconnect() end

    InstaTpConnection = RunService.Stepped:Connect(function()
        if not InstaTeleportEnabled or not SelectedPlayer or SafeZoneActive then return end

        pcall(function()
            local LocalPlayer = Players.LocalPlayer
            local char         = LocalPlayer.Character
            local target       = Players:FindFirstChild(SelectedPlayer)

            if char and target and target.Character then
                local hrp       = char:FindFirstChild("HumanoidRootPart")
                local targetHRP = target.Character:FindFirstChild("UpperTorso")

                if hrp and targetHRP then
                    if not PlayerPositionHistory[SelectedPlayer] then
                        PlayerPositionHistory[SelectedPlayer] = {
                            positions  = {},
                            timestamps = {}
                        }
                    end

                    local history     = PlayerPositionHistory[SelectedPlayer]
                    local currentTime = tick()
                    local currentPos  = targetHRP.Position

                    table.insert(history.positions,  currentPos)
                    table.insert(history.timestamps, currentTime)

                    while #history.positions > PREDICTION_SAMPLES do
                        table.remove(history.positions,  1)
                        table.remove(history.timestamps, 1)
                    end

                    local predictedPos = currentPos

                    if #history.positions >= 2 then
                        local totalDisplacement = Vector3.new(0, 0, 0)
                        local totalTime         = 0

                        for i = 2, #history.positions do
                            local displacement = history.positions[i]  - history.positions[i-1]
                            local deltaTime    = history.timestamps[i] - history.timestamps[i-1]
                            if deltaTime > 0 then
                                totalDisplacement += displacement
                                totalTime         += deltaTime
                            end
                        end

                        if totalTime > 0 then
                            local velocity = totalDisplacement / totalTime
                            predictedPos   = currentPos + (velocity * PREDICTION_TIME)
                        end
                    end

                    hrp.CFrame = CFrame.new(predictedPos) * CFrame.new(XOffset or 0, YOffset or 0, ZOffset or 0)
                else
                    PlayerPositionHistory[SelectedPlayer] = nil
                end
            end
        end)
    end)
end

--  Spectate 
local function StartSpectating()
    local PlayersS    = game:GetService("Players")
    local LocalPlayer = PlayersS.LocalPlayer
    local Camera      = workspace.CurrentCamera

    if not OriginalCamera then
        OriginalCamera = Camera.CameraSubject
    end

    if SpectateConnection then SpectateConnection:Disconnect() end

    SpectateConnection = RunService.RenderStepped:Connect(function()
        if not SpectateEnabled or not SelectedPlayer then
            StopSpectating()
            return
        end

        pcall(function()
            local target = PlayersS:FindFirstChild(SelectedPlayer)
            if target and target.Character then
                local targetHumanoid = target.Character:FindFirstChild("Humanoid")
                if targetHumanoid then
                    Camera.CameraSubject = targetHumanoid
                else
                    StopSpectating()
                end
            else
                StopSpectating()
            end
        end)
    end)
end

function StopSpectating()
    local PlayersS    = game:GetService("Players")
    local LocalPlayer = PlayersS.LocalPlayer
    local Camera      = workspace.CurrentCamera

    if SpectateConnection then
        SpectateConnection:Disconnect()
        SpectateConnection = nil
    end

    if OriginalCamera then
        pcall(function() Camera.CameraSubject = OriginalCamera end)
    else
        pcall(function()
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
                Camera.CameraSubject = LocalPlayer.Character.Humanoid
            end
        end)
    end
    OriginalCamera = nil
end

--  Tabs de la UI 
local Tab  = Window:CreateTab("niggers", gem)
local Tab2 = Window:CreateTab("killaura", gem)
local Tab4 = Window:CreateTab("misc",     gem)
local Tab5 = Window:CreateTab("tps", gem)
local Tab6 = Window:CreateTab("ESP",      gem)

--  Tab ESP 
Tab6:CreateToggle({ Name = "Enable ESP",  CurrentValue = false, Callback = function(v) ESPEnabled  = v end })
Tab6:CreateToggle({ Name = "Names",       CurrentValue = false, Callback = function(v) ESPNames    = v end })
Tab6:CreateToggle({ Name = "Distance",    CurrentValue = false, Callback = function(v) ESPDistance = v end })
Tab6:CreateSlider({
    Name = "Max Distance", Range = {100, 99999999}, Increment = 100,
    CurrentValue = 99999999,
    Callback = function(v) ESPMaxDistance = v end
})

--  ESP render loop 
local Camera         = workspace.CurrentCamera
local LocalPlayerESP = Players.LocalPlayer
local ESPObjects     = {}

local function NewDraw(type, props)
    local d = Drawing.new(type)
    for i,v in pairs(props) do d[i] = v end
    return d
end

local function ClearESP(plr)
    if ESPObjects[plr] then
        for _,obj in pairs(ESPObjects[plr]) do obj:Remove() end
        ESPObjects[plr] = nil
    end
end

local function GetLevel(plr)
    local data = plr:FindFirstChild("Data")
    if data then
        local level = data:FindFirstChild("Level")
        if level and level.Value then return tostring(level.Value) end
    end
    return nil
end

local function GetExactLocation(plr)
    local attr = plr:GetAttribute("ExactLocation")
    if attr ~= nil then return tostring(attr) end
    local v = plr:FindFirstChild("ExactLocation")
    if v and v.Value ~= nil then return tostring(v.Value) end
    local ls = plr:FindFirstChild("leaderstats")
    if ls then
        local lv = ls:FindFirstChild("ExactLocation")
        if lv and lv.Value ~= nil then return tostring(lv.Value) end
    end
    return nil
end

RunService.RenderStepped:Connect(function()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr == LocalPlayerESP then ClearESP(plr); continue end

        local char = plr.Character
        local hrp  = char and char:FindFirstChild("HumanoidRootPart")
        local hum  = char and char:FindFirstChild("Humanoid")

        if not ESPEnabled or not hrp or not hum or hum.Health <= 0 then
            ClearESP(plr); continue
        end

        local dist = (Camera.CFrame.Position - hrp.Position).Magnitude
        if dist > ESPMaxDistance then ClearESP(plr); continue end

        local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
        if not onScreen then ClearESP(plr); continue end

        ESPObjects[plr] = ESPObjects[plr] or {}

        if ESPNames or ESPDistance then
            ESPObjects[plr].Text = ESPObjects[plr].Text or NewDraw("Text", {
                Size    = 16,
                Center  = true,
                Outline = true,
                Color   = Color3.new(1,1,1)
            })

            local txt = ""

            if ESPNames then
                txt = plr.Name
                local level = GetLevel(plr)
                if level then txt ..= " [Lvl " .. level .. "]" end
                local exactLoc = GetExactLocation(plr)
                if exactLoc then txt ..= " [" .. exactLoc .. "]" end
            end

            if ESPDistance then
                txt ..= " (" .. math.floor(dist / 10) .. "m)"
            end

            ESPObjects[plr].Text.Text     = txt
            ESPObjects[plr].Text.Position = Vector2.new(pos.X, pos.Y - 25)
            ESPObjects[plr].Text.Visible  = true
        elseif ESPObjects[plr].Text then
            ESPObjects[plr].Text.Visible = false
        end
    end
end)

Players.PlayerRemoving:Connect(ClearESP)

--  Dropdown selector de jugador 
local PlayerDropdown = Tab:CreateDropdown({
    Name           = "Player Selection",
    Options        = GetPlayerList(),
    CurrentOption  = {"None"},
    Flag           = "PlayerDropdown",
    Callback       = function(opt)
        if type(opt) == "table" then opt = opt[1] end
        SelectedPlayer = (opt ~= "None") and opt or nil
    end,
})

--  Tab Players 
Tab:CreateToggle({
    Name = "Tween To FuckNigga", CurrentValue = false, Flag = "TPToggle",
    Callback = function(v)
        TeleportEnabled = v
        if v then
            if SelectedPlayer then StartTeleporting() end
        else
            if TeleportConnection then TeleportConnection:Disconnect() end
            if ActiveTween then ActiveTween:Cancel() end
            ActiveTween = nil
        end
    end,
})

Tab:CreateToggle({
    Name = "tp bypass 67", CurrentValue = false, Flag = "InstaTpToggle",
    Callback = function(v)
        InstaTeleportEnabled = v
        if v then
            if SelectedPlayer then StartInstaTeleporting() end
        else
            if InstaTpConnection then InstaTpConnection:Disconnect(); InstaTpConnection = nil end
        end
    end,
})

Tab:CreateToggle({
    Name = "miralo perra", CurrentValue = false, Flag = "SpectateToggle",
    Callback = function(v)
        SpectateEnabled = v
        if v then
            if SelectedPlayer then StartSpectating() end
        else
            StopSpectating()
        end
    end,
})

Tab:CreateButton({
    Name = "refresh lol",
    Callback = function() PlayerDropdown:Refresh(GetPlayerList(), true) end
})

Tab:CreateSlider({ Name = "Y Offset", Range = {0,250}, Increment = 1, Suffix = "", CurrentValue = 0, Flag = "YOffsetSlider", Callback = function(Value) YOffset = Value end })
Tab:CreateSlider({ Name = "X Offset", Range = {0,250}, Increment = 1, Suffix = "", CurrentValue = 0, Flag = "XOffsetSlider", Callback = function(Value) XOffset = Value end })
Tab:CreateSlider({ Name = "Z Offset", Range = {0,250}, Increment = 1, Suffix = "", CurrentValue = 0, Flag = "ZOffsetSlider", Callback = function(Value) ZOffset = Value end })
Tab:CreateSlider({ Name = "Prediction Time (DEBUG)", Range = {0,1}, Increment = 0.1, CurrentValue = 0.3, Callback = function(v) PREDICTION_TIME = v end })
Tab:CreateSlider({ Name = "Prediction Sample (DEBUG)", Range = {1,10}, Increment = 1, CurrentValue = 3,   Callback = function(v) PREDICTION_SAMPLES = v end })

--  Funcin atacar mltiples targets 
local function AttackMultipleTargets(targets)
    pcall(function()
        if not targets or #targets == 0 then return end

        local allTargets = {}
        for _, targetChar in pairs(targets) do
            local head = targetChar:FindFirstChild("Head")
            if head then table.insert(allTargets, {targetChar, head}) end
        end
        if #allTargets == 0 then return end

        local attackArgs = {0}
        RegisterAttack:FireServer(unpack(attackArgs))

        local hitArgs = {allTargets[1][2], allTargets}
        RegisterHit:FireServer(unpack(hitArgs))
    end)
end

--  Tab Killaura 
Tab2:CreateToggle({
    Name = "fast att ", CurrentValue = false,
    Callback = function(v)
        FastAttackEnabled = v
        if v then
            if FastAttackConnection then task.cancel(FastAttackConnection) end
            FastAttackConnection = task.spawn(function()
                while FastAttackEnabled do
                    task.wait(0.01)
                    local myChar = Players.LocalPlayer.Character
                    local myHRP  = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    if not myHRP then continue end

                    local targetsInRange = {}

                    -- Jugadores
                    for _, player in pairs(Players:GetPlayers()) do
                        if player ~= Players.LocalPlayer and player.Character then
                            local humanoid = player.Character:FindFirstChild("Humanoid")
                            local hrp      = player.Character:FindFirstChild("HumanoidRootPart")
                            if humanoid and hrp and humanoid.Health > 0 then
                                local dist = (hrp.Position - myHRP.Position).Magnitude
                                if dist <= FastAttackRange then
                                    table.insert(targetsInRange, player.Character)
                                end
                            end
                        end
                    end

                    -- NPCs (carpeta Enemies)
                    local enemiesFolder = workspace:FindFirstChild("Enemies")
                    if enemiesFolder then
                        for _, npc in pairs(enemiesFolder:GetChildren()) do
                            local humanoid = npc:FindFirstChild("Humanoid")
                            local hrp      = npc:FindFirstChild("HumanoidRootPart")
                            if humanoid and hrp and humanoid.Health > 0 then
                                local dist = (hrp.Position - myHRP.Position).Magnitude
                                if dist <= FastAttackRange then
                                    table.insert(targetsInRange, npc)
                                end
                            end
                        end
                    end

                    if #targetsInRange > 0 then
                        AttackMultipleTargets(targetsInRange)
                    end
                end
            end)
        else
            if FastAttackConnection then task.cancel(FastAttackConnection); FastAttackConnection = nil end
        end
    end,
})

--  Funcin obtener Demon Fruit del jugador 
local function GetPlayerFruit()
    local backpack = lp.Backpack
    for _, item in ipairs(backpack:GetChildren()) do
        if item:GetAttribute("WeaponType") == "Demon Fruit" then return item end
    end
    for _, item in ipairs(lp.Character:GetChildren()) do
        if item:GetAttribute("WeaponType") == "Demon Fruit" then return item end
    end
    return nil
end

Tab2:CreateToggle({
    Name = "fruit Aura", CurrentValue = false,
    Callback = function(vatt)
        FruitAttack = vatt
        if vatt then
            if FruitAttackConnection then task.cancel(FruitAttackConnection) end
            FruitAttackConnection = task.spawn(function()
                while FruitAttack do
                    task.wait(0.1)
                    local args = {vector.create(0.12, -0.5, -0.1), 1, true}
                    GetPlayerFruit():WaitForChild("LeftClickRemote"):FireServer(unpack(args))
                end
            end)
        else
            if FruitAttackConnection then task.cancel(FruitAttackConnection); FruitAttackConnection = nil end
        end
    end,
})

Tab2:CreateToggle({
    Name = "Fast Attack (Players)", CurrentValue = false,
    Callback = function(v)
        FastAttackEnabled = v
        if v then
            if FastAttackConnection then task.cancel(FastAttackConnection) end
            FastAttackConnection = task.spawn(function()
                while FastAttackEnabled do
                    task.wait(0.01)
                    local myChar = Players.LocalPlayer.Character
                    local myHRP  = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    if not myHRP then continue end

                    local targetsInRange = {}
                    for _, player in pairs(Players:GetPlayers()) do
                        if player ~= Players.LocalPlayer and player.Character then
                            local humanoid = player.Character:FindFirstChild("Humanoid")
                            local hrp      = player.Character:FindFirstChild("HumanoidRootPart")
                            if humanoid and hrp and humanoid.Health > 0 then
                                local dist = (hrp.Position - myHRP.Position).Magnitude
                                if dist <= FastAttackRange then
                                    table.insert(targetsInRange, player.Character)
                                end
                            end
                        end
                    end

                    if #targetsInRange > 0 then
                        AttackMultipleTargets(targetsInRange)
                    end
                end
            end)
        else
            if FastAttackConnection then task.cancel(FastAttackConnection); FastAttackConnection = nil end
        end
    end,
})

--  Tab Misc 
Tab4:CreateToggle({
    Name = "Anti Mover", CurrentValue = false,
    Callback = function(v)
        local player = Players.LocalPlayer
        if v then
            local function add(char)
                if not char:FindFirstChild("AntiMover") then
                    Instance.new("Folder", char).Name = "AntiMover"
                end
            end
            if player.Character then add(player.Character) end
            _G.AntiMoverConnection = player.CharacterAdded:Connect(add)
        else
            if _G.AntiMoverConnection then _G.AntiMoverConnection:Disconnect() end
            if player.Character and player.Character:FindFirstChild("AntiMover") then
                player.Character.AntiMover:Destroy()
            end
        end
    end,
})

Tab4:CreateToggle({
    Name = "Unbreakable", CurrentValue = false,
    Callback = function(skibidi)
        Unbreakable = skibidi
        if skibidi then
            if UnbreakableConnection then task.cancel(UnbreakableConnection) end
            UnbreakableConnection = task.spawn(function()
                while Unbreakable do
                    task.wait(0.1)
                    if Players.LocalPlayer.Character:GetAttribute("Unbreakable") ~= true then
                        Players.LocalPlayer.Character:SetAttribute("UnbreakableAll", true)
                    end
                end
            end)
        else
            if UnbreakableConnection then
                task.cancel(UnbreakableConnection)
                Players.LocalPlayer.Character:SetAttribute("UnbreakableAll", false)
                UnbreakableConnection = nil
            end
        end
    end,
})

Tab4:CreateToggle({
    Name = "Auto V4", CurrentValue = false,
    Callback = function(papuxx)
        autoV4 = papuxx
        if papuxx then
            if v4Connection then task.cancel(v4Connection) end
            v4Connection = task.spawn(function()
                while autoV4 do
                    task.wait(0.5)
                    local args = {true}
                    game:GetService("Players").LocalPlayer
                        :WaitForChild("Backpack")
                        :WaitForChild("Awakening")
                        :WaitForChild("RemoteFunction")
                        :InvokeServer(unpack(args))
                end
            end)
        else
            if v4Connection then task.cancel(v4Connection); v4Connection = nil end
        end
    end,
})

Tab4:CreateToggle({
    Name = "Dash Length", CurrentValue = false,
    Callback = function(dashsh)
        DashEnabled = dashsh
        if dashsh then
            if DashConnection then task.cancel(DashConnection) end
            DashConnection = task.spawn(function()
                while DashEnabled do
                    task.wait(0.1)
                    local character = Players.LocalPlayer.Character
                    if character then
                        local currentValue = character:GetAttribute("DashLength")
                        if currentValue ~= DashLenghDistance420 then
                            character:SetAttribute("DashLength",    DashLenghDistance420)
                            character:SetAttribute("DashLengthAir", DashLenghDistance420)
                        end
                    end
                end
            end)
        else
            if DashConnection then
                Players.LocalPlayer.Character:SetAttribute("DashLength",    1)
                Players.LocalPlayer.Character:SetAttribute("DashLengthAir", 1)
                task.cancel(DashConnection)
                DashConnection = nil
            end
        end
    end,
})

Tab4:CreateSlider({
    Name = "Max Distance", Range = {1, 300}, Increment = 1, CurrentValue = 1,
    Callback = function(v) DashLenghDistance420 = v end
})

--  Tab Teleports (botones a lugares del mapa) 
Tab5:CreateButton({ Name = "Mansion",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-12463, 376, -7567)
    end })

Tab5:CreateButton({ Name = "Angel Island (Sea 1)",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-4627.80517578125, 848.034973144531, -1706.73498535156)
    end })

Tab5:CreateButton({ Name = "Flamingo (Inside)",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(2285, 38, 899)
    end })

Tab5:CreateButton({ Name = "Flamingo (Outside)",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-287, 306, 598)
    end })

Tab5:CreateButton({ Name = "Haunted Ship (Outside)",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-6500, 129, -123)
    end })

Tab5:CreateButton({ Name = "Haunted Ship (Inside)",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(918.575561523437, 125.098335266113, 32851.52734375)
    end })

Tab5:CreateButton({ Name = "Tiki",
    Callback = function()
        Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-16801.0859375, 58.7130851745605, 306.069946289062)
    end })

Tab5:CreateButton({ Name = "Remove TouchInterest",
    Callback = function()
        for _, descendant in pairs(game:GetDescendants()) do
            if descendant:IsA("TouchTransmitter") then
                descendant:Destroy()
            end
        end
    end })

--  Hotkey B: toggle fly infinito 
local toggled = false
UIS.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode ~= Enum.KeyCode.B then return end

    toggled = not toggled

    local char = Players.LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if not toggled then
        local flag = hrp:FindFirstChild("UpLoop")
        if flag then flag:Destroy() end
        return
    end

    local flag = Instance.new("BoolValue")
    flag.Name   = "UpLoop"
    flag.Parent = hrp

    task.spawn(function()
        while flag.Parent and toggled do
            local pos     = hrp.Position
            local randomY = math.random(2000, 9999999)
            hrp.CFrame    = CFrame.new(pos.X, randomY, pos.Z)
            task.wait(0.001)
        end
    end)
end)

--  Hotkey N: noclip / cada infinita 
local oneenabledshothoo = false
UIS.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode ~= Enum.KeyCode.N then return end

    local char = Players.LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    oneenabledshothoo = not oneenabledshothoo

    if oneenabledshothoo then
        task.spawn(function()
            while oneenabledshothoo do
                local pos = hrp.Position
                hrp.CFrame = CFrame.new(pos.X, pos.Y - 79567969579632679567969579632, pos.Z)
                task.wait(0.001)
            end
        end)
    end
end)]])

local _call8 = loadstring(game:HttpGet('https://sirius.menu/rayfield'))():CreateWindow({
    LoadingTitle = 'Cagada',
    Name = 'Nexxuzz Hub Pub',
    Discord = {Enabled = false},
    KeySystem = false,
    LoadingSubtitle = 'skidded code 67',
    Icon = 'gem',
    ConfigurationSaving = {
        Enabled = false,
        FileName = 'Nexxuzzzzzzzzz',
    },
})
local _call10 = game:GetService('Players')
local _call14 = game:GetService('UserInputService')
local _call16 = game:GetService('RunService')
local _call18 = game:GetService('TweenService')
local _ = _call10.LocalPlayer.Character.HumanoidRootPart
local _Net23 = game:GetService('ReplicatedStorage').Modules.Net
local _ = _Net23['RE/RegisterHit']
local _ = _Net23['RE/RegisterAttack']
local _call27 = _call8:CreateTab('niggers', nil)
local _call29 = _call8:CreateTab('killaura', nil)
local _call31 = _call8:CreateTab('misc', nil)
local _call33 = _call8:CreateTab('tps', nil)
local _call35 = _call8:CreateTab('ESP', nil)

_call35:CreateToggle({
    CurrentValue = false,
    Name = 'Enable ESP',
    Callback = function(_38) end,
})
_call35:CreateToggle({
    CurrentValue = false,
    Name = 'Names',
    Callback = function(_41) end,
})
_call35:CreateToggle({
    CurrentValue = false,
    Name = 'Distance',
    Callback = function(_44) end,
})
_call35:CreateSlider({
    Name = 'Max Distance',
    CurrentValue = 99999999,
    Increment = 100,
    Range = {
        [1] = 100,
        [2] = 99999999,
    },
    Callback = function(_47) end,
})

local _ = workspace.CurrentCamera

_call16.RenderStepped:Connect(function()
    for _56, _56_2 in ipairs(_call10:GetPlayers())do
        local _ = _56_2 == _call10.LocalPlayer
        local _Character58 = _56_2.Character

        _Character58:FindFirstChild('HumanoidRootPart')

        local _ = _Character58:FindFirstChild('Humanoid').Health

        error('[string "25ms_loadstring"]:361: attempt to compare table <= number')
    end
end)
_call10.PlayerRemoving:Connect(function(_67) end)

for _70, _70_2 in pairs(_call10:GetPlayers())do
    local _ = _70_2 == _call10.LocalPlayer
end

local _call75 = _call27:CreateDropdown({
    Name = 'Player Selection',
    CurrentOption = {
        [1] = 'None',
    },
    Flag = 'PlayerDropdown',
    Callback = function(_76)
        type(_76)

        local _ = _76[1]
    end,
    Options = {
        [1] = _70_2.Name,
    },
})

_call27:CreateToggle({
    CurrentValue = false,
    Callback = function(_80)
        _call16.Heartbeat:Connect(function()
            local _call86 = _call10:FindFirstChild(_Idx77)
            local _ = _call86.Character
            local _call90 = _call86.Character:FindFirstChild('HumanoidRootPart')
            local _LocalPlayer91 = _call10.LocalPlayer
            local _ = _LocalPlayer91.Character
            local _call95 = _LocalPlayer91.Character:FindFirstChild('HumanoidRootPart')
            local _call98 = _LocalPlayer91.Character:FindFirstChild('Humanoid')

            _call95.AssemblyLinearVelocity = Vector3.zero
            _call95.AssemblyAngularVelocity = Vector3.zero

            _call98:ChangeState(Enum.HumanoidStateType.Physics)

            local _111 = math.rad(_call90.Orientation.Y)
            local _116 = ((CFrame.new(_call90.Position) * CFrame.Angles(0, _111, 0)) * CFrame.new(nil, nil, nil))
            local _134 = math.sqrt((((_116.Position.X - _call95.Position.X) ^ 2) + ((_116.Position.Z - _call95.Position.Z) ^ 2)))
            local _140 = math.abs((_116.Position.Y - _call95.Position.Y))
            local _ = _134 < _140
            local _call148 = _call18:Create(_call95, TweenInfo.new(((_116.Position - _call95.Position).Magnitude / 350), Enum.EasingStyle.Linear), {CFrame = _116})

            task.spawn(function()
                for _156, _156_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _156_2:IsA('BasePart')

                    _156_2.CanCollide = false
                    _156_2.CanTouch = true
                    _156_2.CanQuery = true
                end

                task.wait(0.001)

                for _163, _163_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _163_2:IsA('BasePart')

                    _163_2.CanCollide = false
                    _163_2.CanTouch = true
                    _163_2.CanQuery = true
                end

                task.wait(0.001)

                for _170, _170_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _170_2:IsA('BasePart')

                    _170_2.CanCollide = false
                    _170_2.CanTouch = true
                    _170_2.CanQuery = true
                end

                task.wait(0.001)

                for _177, _177_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _177_2:IsA('BasePart')

                    _177_2.CanCollide = false
                    _177_2.CanTouch = true
                    _177_2.CanQuery = true
                end

                task.wait(0.001)

                for _184, _184_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _184_2:IsA('BasePart')

                    _184_2.CanCollide = false
                    _184_2.CanTouch = true
                    _184_2.CanQuery = true
                end

                task.wait(0.001)

                for _191, _191_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _191_2:IsA('BasePart')

                    _191_2.CanCollide = false
                    _191_2.CanTouch = true
                    _191_2.CanQuery = true
                end

                task.wait(0.001)

                for _198, _198_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _198_2:IsA('BasePart')

                    _198_2.CanCollide = false
                    _198_2.CanTouch = true
                    _198_2.CanQuery = true
                end

                task.wait(0.001)

                for _205, _205_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _205_2:IsA('BasePart')

                    _205_2.CanCollide = false
                    _205_2.CanTouch = true
                    _205_2.CanQuery = true
                end

                task.wait(0.001)

                for _212, _212_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _212_2:IsA('BasePart')

                    _212_2.CanCollide = false
                    _212_2.CanTouch = true
                    _212_2.CanQuery = true
                end

                task.wait(0.001)

                for _219, _219_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _219_2:IsA('BasePart')

                    _219_2.CanCollide = false
                    _219_2.CanTouch = true
                    _219_2.CanQuery = true
                end

                task.wait(0.001)

                for _226, _226_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _226_2:IsA('BasePart')

                    _226_2.CanCollide = false
                    _226_2.CanTouch = true
                    _226_2.CanQuery = true
                end

                task.wait(0.001)

                for _233, _233_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _233_2:IsA('BasePart')

                    _233_2.CanCollide = false
                    _233_2.CanTouch = true
                    _233_2.CanQuery = true
                end

                task.wait(0.001)

                for _240, _240_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _240_2:IsA('BasePart')

                    _240_2.CanCollide = false
                    _240_2.CanTouch = true
                    _240_2.CanQuery = true
                end

                task.wait(0.001)

                for _247, _247_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _247_2:IsA('BasePart')

                    _247_2.CanCollide = false
                    _247_2.CanTouch = true
                    _247_2.CanQuery = true
                end

                task.wait(0.001)

                for _254, _254_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    _254_2:IsA('BasePart')

                    _254_2.CanCollide = false
                    _254_2.CanTouch = true
                    _254_2.CanQuery = true
                end

                task.wait(0.001)

                local _ = _call10.LocalPlayer

                error('internal 579: <25ms: infinitelooperror>')
            end)
            _call148:Play()
            _call148.Completed:Connect(function()
                for _269, _269_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                    local _ = _269_2.IsA

                    error('internal 579: <25ms: infinitelooperror>')
                end
            end)
            task.delay(0.05, function()
                _call98:ChangeState(Enum.HumanoidStateType.Running)
            end)

            for _283, _283_2 in ipairs(_call10.LocalPlayer.Character:GetChildren())do
                _283_2:IsA('BasePart')

                _283_2.CanCollide = false
                _283_2.CanTouch = true
                _283_2.CanQuery = true
            end
        end)
    end,
    Name = 'Tween To FuckNigga',
    Flag = 'TPToggle',
})
_call27:CreateToggle({
    CurrentValue = false,
    Callback = function(_288)
        _call16.Stepped:Connect(function()
            local _Character294 = _call10.LocalPlayer.Character
            local _call296 = _call10:FindFirstChild(_Idx77)
            local _ = _call296.Character
            local _call299 = _Character294:FindFirstChild('HumanoidRootPart')

            _call299.CFrame = (CFrame.new(_call296.Character:FindFirstChild('UpperTorso').Position) * CFrame.new(0, 0, 0))
        end)
    end,
    Name = 'tp bypass 67',
    Flag = 'InstaTpToggle',
})
_call27:CreateToggle({
    CurrentValue = false,
    Callback = function(_311)
        local _call313 = game:GetService('Players')
        local _ = _call313.LocalPlayer
        local _workspaceCurrentCamera315 = workspace.CurrentCamera
        local _ = _workspaceCurrentCamera315.CameraSubject

        _call16.RenderStepped:Connect(function()
            local _call322 = _call313:FindFirstChild(_Idx77)
            local _ = _call322.Character

            _workspaceCurrentCamera315.CameraSubject = _call322.Character:FindFirstChild('Humanoid')
        end)
    end,
    Name = 'miralo perra',
    Flag = 'SpectateToggle',
})
_call27:CreateButton({
    Name = 'refresh lol',
    Callback = function()
        for _332, _332_2 in pairs(_call10:GetPlayers())do
            local _ = _332_2 == _call10.LocalPlayer
        end

        _call75:Refresh({
            [1] = _332_2.Name,
        }, true)
    end,
})
_call27:CreateSlider({
    Callback = function(_340) end,
    Name = 'Y Offset',
    Suffix = '',
    CurrentValue = 0,
    Increment = 1,
    Range = {
        [1] = 0,
        [2] = 250,
    },
    Flag = 'YOffsetSlider',
})
_call27:CreateSlider({
    Callback = function(_343) end,
    Name = 'X Offset',
    Suffix = '',
    CurrentValue = 0,
    Increment = 1,
    Range = {
        [1] = 0,
        [2] = 250,
    },
    Flag = 'XOffsetSlider',
})
_call27:CreateSlider({
    Callback = function(_346) end,
    Name = 'Z Offset',
    Suffix = '',
    CurrentValue = 0,
    Increment = 1,
    Range = {
        [1] = 0,
        [2] = 250,
    },
    Flag = 'ZOffsetSlider',
})
_call27:CreateSlider({
    Name = 'Prediction Time (DEBUG)',
    CurrentValue = 0.3,
    Increment = 0.1,
    Range = {
        [1] = 0,
        [2] = 1,
    },
    Callback = function(_349) end,
})
_call27:CreateSlider({
    Name = 'Prediction Sample (DEBUG)',
    CurrentValue = 3,
    Increment = 1,
    Range = {
        [1] = 1,
        [2] = 10,
    },
    Callback = function(_352) end,
})
_call29:CreateToggle({
    CurrentValue = false,
    Name = 'fast att ',
    Callback = function(_355)
        task.spawn(function()
            task.wait(0.01)
            _call10.LocalPlayer.Character:FindFirstChild('HumanoidRootPart')

            for _365, _365_2 in pairs(_call10:GetPlayers())do
                local _ = _365_2 == _call10.LocalPlayer
                local _ = _365_2.Character
                local _Character369 = _365_2.Character

                _365_2.Character:FindFirstChild('HumanoidRootPart')

                local _ = _Character369:FindFirstChild('Humanoid').Health

                error('[string "25ms_loadstring"]:509: attempt to compare number < table')
            end
        end)
    end,
})
_call29:CreateToggle({
    CurrentValue = false,
    Name = 'fruit Aura',
    Callback = function(_378)
        task.spawn(function()
            task.wait(0.1)
            error('[string "25ms_loadstring"]:565: attempt to index function with \'create\'')
        end)
    end,
})
_call29:CreateToggle({
    CurrentValue = false,
    Name = 'Fast Attack (Players)',
    Callback = function(_384)
        task.cancel(_call357)
        task.spawn(function()
            task.wait(0.01)
            _call10.LocalPlayer.Character:FindFirstChild('HumanoidRootPart')

            for _396, _396_2 in pairs(_call10:GetPlayers())do
                local _ = _396_2 == _call10.LocalPlayer
                local _ = _396_2.Character
                local _Character400 = _396_2.Character

                _396_2.Character:FindFirstChild('HumanoidRootPart')

                local _ = _Character400:FindFirstChild('Humanoid').Health

                error('[string "25ms_loadstring"]:593: attempt to compare number < table')
            end
        end)
    end,
})
_call31:CreateToggle({
    CurrentValue = false,
    Name = 'Anti Mover',
    Callback = function(_409)
        local _LocalPlayer410 = _call10.LocalPlayer
        local _ = _LocalPlayer410.Character

        _LocalPlayer410.Character:FindFirstChild('AntiMover')

        local _call417 = _LocalPlayer410.CharacterAdded:Connect(function(_418)
            _418:FindFirstChild('AntiMover')
        end)

        _G.AntiMoverConnection = _call417
    end,
})
_call31:CreateToggle({
    CurrentValue = false,
    Name = 'Unbreakable',
    Callback = function(_423)
        task.spawn(function()
            error('internal 579: <25ms: infinitelooperror>')
        end)
    end,
})
_call31:CreateToggle({
    CurrentValue = false,
    Name = 'Auto V4',
    Callback = function(_429)
        task.spawn(function()
            task.wait(0.5)
            game:GetService('Players').LocalPlayer:WaitForChild('Backpack')
            error('internal 579: <25ms: infinitelooperror>')
        end)
    end,
})
_call31:CreateToggle({
    CurrentValue = false,
    Name = 'Dash Length',
    Callback = function(_441)
        task.spawn(function()
            error('internal 579: <25ms: infinitelooperror>')
        end)
    end,
})
_call31:CreateSlider({
    Name = 'Max Distance',
    CurrentValue = 1,
    Increment = 1,
    Range = {
        [1] = 1,
        [2] = 300,
    },
    Callback = function(_447) end,
})
_call33:CreateButton({
    Name = 'Mansion',
    Callback = function()
        local _HumanoidRootPart453 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart453.CFrame = CFrame.new(-12463, 376, -7567)
    end,
})
_call33:CreateButton({
    Name = 'Angel Island (Sea 1)',
    Callback = function()
        local _HumanoidRootPart461 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart461.CFrame = CFrame.new(-4627.80517578125, 848.034973144531, -1706.73498535156)
    end,
})
_call33:CreateButton({
    Name = 'Flamingo (Inside)',
    Callback = function()
        local _HumanoidRootPart469 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart469.CFrame = CFrame.new(2285, 38, 899)
    end,
})
_call33:CreateButton({
    Name = 'Flamingo (Outside)',
    Callback = function()
        local _HumanoidRootPart477 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart477.CFrame = CFrame.new(-287, 306, 598)
    end,
})
_call33:CreateButton({
    Name = 'Haunted Ship (Outside)',
    Callback = function()
        local _HumanoidRootPart485 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart485.CFrame = CFrame.new(-6500, 129, -123)
    end,
})
_call33:CreateButton({
    Name = 'Haunted Ship (Inside)',
    Callback = function()
        local _HumanoidRootPart493 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart493.CFrame = CFrame.new(918.575561523437, 125.098335266113, 32851.52734375)
    end,
})
_call33:CreateButton({
    Name = 'Tiki',
    Callback = function()
        local _HumanoidRootPart501 = _call10.LocalPlayer.Character.HumanoidRootPart

        _HumanoidRootPart501.CFrame = CFrame.new(-16801.0859375, 58.7130851745605, 306.069946289062)
    end,
})
_call33:CreateButton({
    Name = 'Remove TouchInterest',
    Callback = function()
        for _509, _509_2 in pairs(game:GetDescendants())do
            _509_2:IsA('TouchTransmitter')
            _509_2:Destroy()
        end
    end,
})
_call14.InputBegan:Connect(function(_517, _517_2) end)
_call14.InputBegan:Connect(function(_521, _521_2) end)
