-- ==================== Blox Fruits 跟随锁定玩家 ====================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

_G.FollowPlayer = false
_G.TargetPlayerName = nil
_G.FollowHeight = 90

-- 获取当前服务器其他玩家列表
local function getPlayerNames()
    local list = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            table.insert(list, p.Name)
        end
    end
    return list
end

-- ==================== WindUI 界面 ====================
local WindUI
pcall(function()
    WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
end)

if not WindUI then
    warn("WindUI 加载失败！")
    return
end

local Window = WindUI:CreateWindow({
    Title = "跟随锁定玩家",
    Icon = "crosshair",
    Author = "kiyo",
    Folder = "WindUI_Follow",
    Size = UDim2.fromOffset(450, 320),
    Transparent = true,
    Theme = "Dark",
    Acrylic = false,
    HideSearchBar = false,
    ToggleKey = Enum.KeyCode.RightControl,
})

local MainTab = Window:Tab({ Title = "跟随控制", Icon = "user" })

MainTab:Section({ Title = "目标设置" })

local playerDropdown = MainTab:Dropdown({
    Title = "选择玩家",
    Values = getPlayerNames(),
    Value = "",
    Callback = function(v)
        _G.TargetPlayerName = v
    end
})

MainTab:Button({
    Title = "刷新玩家列表",
    Callback = function()
        playerDropdown:Refresh(getPlayerNames())
    end
})

MainTab:Section({ Title = "锁定设置" })

MainTab:Slider({
    Title = "跟随高度 (Studs)",
    Value = { Min = 10, Max = 300, Default = 90 },
    Callback = function(v)
        _G.FollowHeight = v
    end
})

MainTab:Toggle({
    Title = "开启锁定跟随",
    Value = _G.FollowPlayer,
    Callback = function(v)
        _G.FollowPlayer = v
    end
})

-- ==================== 锁定与防掉落逻辑 ====================
local noclipConnection
local followConnection
local platform

-- 核心修复：使用 Vector3Value 承载 Tween
-- 直接 Tween HRP 的 CFrame 会导致引擎强制插值旋转，从而出现“本地假视角，别人真飞”的现象
local followPosValue = Instance.new("Vector3Value")
followPosValue.Value = Vector3.zero

local function createPlatform()
    if not platform or not platform.Parent then
        platform = Instance.new("Part")
        platform.Name = "FollowPlatform_Kiyo"
        platform.Size = Vector3.new(10, 1, 10)
        platform.Transparency = 1
        platform.Anchored = true
        platform.CanCollide = true
        platform.Parent = Workspace
    end
    return platform
end

local function enableFollow()
    if noclipConnection then noclipConnection:Disconnect() end
    if followConnection then followConnection:Disconnect() end
    if platform then platform:Destroy() platform = nil end

    local currentTween = nil
    local lastTweenDest = nil
    local followSpeed = 250
    local recalcThreshold = 25

    -- 初始化位置值为玩家当前位置
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if root then
        followPosValue.Value = root.Position
    end

    -- 开启无碰撞与关闭自动转向
    noclipConnection = RunService.Stepped:Connect(function()
        if _G.FollowPlayer then
            local char = LocalPlayer.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then hum.AutoRotate = false end
                
                for _, v in ipairs(char:GetDescendants()) do
                    if v:IsA("BasePart") and v.CanCollide and v.Name ~= "FollowPlatform_Kiyo" then
                        v.CanCollide = false
                    end
                end
            end
        end
    end)

    -- 严格遵循规则的跟随逻辑
    followConnection = RunService.Heartbeat:Connect(function()
        if _G.FollowPlayer and _G.TargetPlayerName then
            local target = Players:FindFirstChild(_G.TargetPlayerName)
            if target and target.Character and target.Character:FindFirstChild("Head") then
                local char = LocalPlayer.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if root then
                    local headPos = target.Character.Head.Position
                    local targetPos = headPos + Vector3.new(0, _G.FollowHeight, 0)
                    
                    local distToTarget = (targetPos - followPosValue.Value).Magnitude
                    local distFromLastDest = lastTweenDest and (targetPos - lastTweenDest).Magnitude or math.huge
                    
                    local shouldCreateTween = false
                    
                    -- 排除极小偏差，避免无限生成微操 Tween
                    if distToTarget > 1 then
                        -- 条件1：目标移动距离超过上次设定的终点 25 Studs
                        if distFromLastDest > recalcThreshold then
                            shouldCreateTween = true
                        -- 条件2：当前没有正在播放的 Tween (自然完成或被取消)
                        elseif not currentTween or not currentTween.Playing then
                            shouldCreateTween = true
                        end
                    end
                    
                    if shouldCreateTween then
                        -- 创建前：清零线性速度
                        root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        
                        -- 时间计算与最小值保护
                        local duration = math.max(0.01, distToTarget / followSpeed)
                        
                        -- 取消上一个残留的 Tween
                        if currentTween then currentTween:Cancel() end
                        
                        -- 对 Vector3Value 使用 Linear 缓动进行匀速补间
                        local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear)
                        currentTween = TweenService:Create(followPosValue, tweenInfo, {Value = targetPos})
                        
                        -- 仅在自然完成时清零双速度防止漂移 (忽略 Cancel 触发的状态)
                        currentTween.Completed:Connect(function(playbackState)
                            if playbackState == Enum.PlaybackState.Completed then
                                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                                root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                            end
                        end)
                        
                        currentTween:Play()
                        lastTweenDest = targetPos
                    end
                    
                    -- 核心：每帧将 Tween 平滑算出的位置，强制结合当前真实的本地鼠标视角
                    local myRotation = root.CFrame.Rotation
                    root.CFrame = CFrame.new(followPosValue.Value) * myRotation
                    
                    -- 实时更新垫板
                    local p = createPlatform()
                    p.CFrame = CFrame.new(root.Position - Vector3.new(0, 3.2, 0))
                end
            end
        else
            -- 关闭时清理状态
            if currentTween then currentTween:Cancel() currentTween = nil end
            lastTweenDest = nil
            
            if platform then platform:Destroy() platform = nil end
            
            local char = LocalPlayer.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then hum.AutoRotate = true end
            end
        end
    end)
end

enableFollow()

print("跟随锁定玩家脚本已加载！")
