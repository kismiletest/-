-- [[ 1.en Hub: ESP MANAGER 最终强化版 ]] --

-- ... (前面的 UI 构建代码保持不变，直到 CreateToggle 部分) ...

CreateToggle("启用透视系统", "ESP_Enabled")
CreateToggle("透视傻逼玩家", "Show_Players")
CreateToggle("透视所有怪物(含NPC)", "Show_Monsters") -- 这里取消了NPC过滤
CreateToggle("显示没脑名字", "Show_Name")
CreateToggle("显示傻逼距离", "Show_Distance")

-- [[ ESP 核心逻辑：移除所有限制条件 ]] --

local function CreateESP(obj, color)
    if obj:FindFirstChild("EnESP") then return end
    
    local Highlight = Instance.new("Highlight", obj)
    Highlight.Name = "EnESP"
    Highlight.FillTransparency = 0.6
    Highlight.FillColor = color
    Highlight.OutlineColor = Color3.new(1, 1, 1)

    local Billboard = Instance.new("BillboardGui", obj)
    Billboard.Name = "EnTag"
    Billboard.Size = UDim2.new(0, 100, 0, 20)
    -- 适配：如果没头就绑在主躯干上
    Billboard.Adornee = obj:FindFirstChild("Head") or obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart
    Billboard.AlwaysOnTop = true
    Billboard.ExtentsOffset = Vector3.new(0, 3, 0)

    local Label = Instance.new("TextLabel", Billboard)
    Label.Size = UDim2.new(1, 0, 1, 0)
    Label.BackgroundTransparency = 1
    Label.TextColor3 = color
    Label.Font = Enum.Font.GothamBold
    Label.TextSize = 12
    Label.Text = ""
    Label.Name = "TagLabel"
end

-- 性能优化处理
local lastTick = 0
RS.RenderStepped:Connect(function()
    -- 彩虹 UI 逻辑省略...
    
    if tick() - lastTick < 0.15 then return end -- 稍微降低频率保护手机
    lastTick = tick()

    if not Settings.ESP_Enabled then
        for _, v in pairs(workspace:GetDescendants()) do
            if v.Name == "EnESP" or v.Name == "EnTag" then v:Destroy() end
        end
        return
    end

    -- 深度遍历：不仅找第一层，防止怪物在文件夹里
    -- 注意：大规模 Descendants 遍历在手机上较卡，建议针对特定文件夹改用 GetChildren
    for _, v in pairs(workspace:GetDescendants()) do 
        if v:IsA("Model") and v ~= LocalPlayer.Character then
            local isPlayer = Players:GetPlayerFromCharacter(v)
            -- 核心修改：只要有 Humanoid 就算怪物，不管是不是 NPC
            local isHumanoid = v:FindFirstChildOfClass("Humanoid") 
            
            local valid = false
            local col = Color3.new(1,1,1)

            if isPlayer and Settings.Show_Players then
                valid = true
                col = Color3.fromRGB(0, 170, 255)
            elseif isHumanoid and not isPlayer and Settings.Show_Monsters then
                valid = true
                col = Color3.fromRGB(255, 40, 40)
            end

            -- 渲染逻辑
            local root = v:FindFirstChild("HumanoidRootPart") or v.PrimaryPart
            if valid and root then
                CreateESP(v, col)
                local tag = v:FindFirstChild("EnTag")
                if tag and tag:FindFirstChild("TagLabel") then
                    local dist = math.floor((LocalPlayer.Character.PrimaryPart.Position - root.Position).Magnitude)
                    tag.TagLabel.Text = (Settings.Show_Name and v.Name or "") .. (Settings.Show_Distance and " ["..dist.."m]" or "")
                end
            else
                -- 如果不再符合条件，则清除
                if v:FindFirstChild("EnESP") then v.EnESP:Destroy() end
                if v:FindFirstChild("EnTag") then v.EnTag:Destroy() end
            end
        end
    end
end)
